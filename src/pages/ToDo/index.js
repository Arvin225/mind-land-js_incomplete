function ToDo(listName) {
    if (listName === '全部') {
        // 展示 列表名 在todo项上
    } else {
        // 不展示列表名
    }

    return (
        <>
            <h1>列表名称</h1>
            {/* 渲染to-do项组件 */}


            {/* to-do input表单 */}
        </>
    )
}

export default ToDo